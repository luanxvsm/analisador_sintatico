function main() {
    let valor_final: number;
    let nota1: number;
    let nota2: number;
    let media: float;
    let offset: number;

    valor_final = nota1 + nota2;
    media = (valor_final / 2) * 10; 
    offset = 0 - 5;
}